#include <stdio.h>

/**
 * main - entry of tge program
 *
 * Return: always 0
 */

int main(void)
{
	long n;
	long m;
	int cnto = 25;
	int cntr;

	for (cntr = 1, cntr <= cnto; cntr++)
	{
		printf("%li %li", a, b);
		a += b;
		b += a;
	}
	if (cnto % 2 == 1)
		printf("%li", a);
	printf("\n");
	
	return (0);
}
